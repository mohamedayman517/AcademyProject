import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// Placeholder Shared UI module for reusable UI elements (buttons, cards, dialogs, etc.)
// Add components/directives/pipes here and export them for reuse across the app.

@NgModule({
  declarations: [],
  imports: [CommonModule],
  exports: []
})
export class UiModule {}
